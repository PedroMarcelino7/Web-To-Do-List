import styled from "styled-components";

export const Input = styled.input`
    display: flex;
    width: 100%;
    font-size: 1rem;
    border: none;

    &::placeholder {
        color: var(--gray600);
    }
`
